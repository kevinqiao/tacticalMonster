import { PageContainer } from "service/PageManager";

export const ChildrenAnimateFunctions = {
    slide: (args: { type: number; container: PageContainer; child: string }) => {
        return
    },
};
