import criticalPlaceholderA from "./assets/critical-placeholder-a.svg";
import criticalPlaceholderB from "./assets/critical-placeholder-b.svg";

/** Critical image URLs for Child2 (Vite-hashed). Replace placeholders when real UI assets exist. */
export const child2CriticalAssets: string[] = [criticalPlaceholderA, criticalPlaceholderB];
